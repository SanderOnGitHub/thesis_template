% \NeedsTeXFormat{LaTeX2e}
% \ProvidesPackage{coverpage}[2020/03/12 v0.1 HAW MP Thesis Style Cover]

% \newif\if@print
% \DeclareOption{print}{\@printtrue}
% \ExecuteOptions{}

\RequirePackage{kvoptions}
\RequirePackage{ifthen}

\PassOptionsToPackage{rgb}{xcolor}
\RequirePackage{xcolor}
\RequirePackage[absolute]{textpos}
\RequirePackage{graphicx}

\SetupKeyvalOptions{
  family=cover,
  prefix=cover@
}

\DeclareBoolOption{print}
\ProcessKeyvalOptions{cover}

\renewcommand*{\maketitle}{
  \begin{titlepage}%
  \begingroup
  \thispagestyle{empty}%
  \enlargethispage{\footskip}%
  \setlength{\parindent}{0em} % remove indent
  \sffamily % the title page in sans serif

  \ifthenelse{\boolean{cover@print}}{
    \colorlet{HAWBannerColor}{white}
  }{
    \definecolor{HAWBannerColor}{rgb}{0.00,0.78,0.54}
  }

  %% use HAW for online version, nothing if the file is to be printed
  \begin{textblock*}{2cm}(10cm,2cm)
    \begin{minipage}[r]{\textwidth}%
      \ifthenelse{\boolean{cover@print}}{ % hide logo if printer is true
        \relax
      }{
        \includegraphics[width=10cm]{HAW_logo}%
      }
    \end{minipage}%
  \end{textblock*}

  %% color bar
  \begin{textblock*}{10cm}(0cm,9cm)
    \colorbox{HAWBannerColor}{%
      %% use a minipage that is the size of the colored banner
      %% center the minipage
      \begin{minipage}[l][10cm][c]{\paperwidth}%
        %% put the box on the right side
        \hspace*{0.175\textwidth}
        \parbox[t]{0.7\textwidth}{%
          {\raggedleft\bfseries\Huge theHAWThesisType

          \vspace{2cm}
          \Large\@author

          \vspace{.5cm}
          \@title

        }}%
      \end{minipage}%
    }
  \end{textblock*}
  %% HAW information
  \newlength{\haw@xpos}\newlength{\haw@fw}
  \setlength{\haw@xpos}{2cm}
  \setlength{\haw@fw}{\paperwidth}
  \addtolength{\haw@fw}{-1.6\haw@xpos}
  \begin{textblock*}{2cm}(\haw@xpos,0.875\paperheight)
    \begin{minipage}[c]{\haw@fw}
      \centering\newlength{\haw@tw}
      \settowidth{\haw@tw}{\normalfont\itshape Fakult\"at Technik und Informatik}
      \parbox[t]{\haw@tw}{%
        \normalfont\itshape%
        Fakult\"at Technik und Informatik\\
        Department Maschinenbau und Produktion
      }\hfill
      \settowidth{\haw@tw}{\normalfont\itshape Faculty of Engineering and Computer Science}
      \parbox[t]{\haw@tw}{%
        \normalfont\itshape%
        Faculty of Engineering and Computer Science\\
        Department of Mechanical Engineering and \\Production Management
      }
    \end{minipage}
  \end{textblock*}
  \null\newpage
  \endgroup
  \end{titlepage}
}